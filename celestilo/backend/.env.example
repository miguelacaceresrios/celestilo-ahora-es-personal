# ===========================================
# DATABASE CONFIGURATION
# ===========================================

# Server name and port (Docker exposes 1433 to the host)
DB_SERVER=localhost,1433

# Database name
DB_NAME=AppDb

# Administrator user credentials
DB_USER=sa
DB_PASSWORD=YourStrongPassword123!

# Provider type (for ASP.NET or ORM like Entity Framework)
DB_PROVIDER=SqlServer

# Connection string ready for Linux (ASP.NET Core can read it directly)
CONNECTION_STRING=Server=localhost;Database=AppDB;User Id=sa;Password=YourStrongPassword123!;Encrypt=False;TrustServerCertificate=True;


# ===========================================
# JWT CONFIGURATION (Authentication)
# ===========================================

# Secret key to sign JWT tokens (generate a new one with: openssl rand -base64 32)
JWT_SECRET_KEY=your-secret-key-here-generate-a-new-one

# Token issuer (your API name)
JWT_ISSUER=ProductAPI

# Token audience (who can use these tokens)
JWT_AUDIENCE=ProductWebClients

# Token expiration time in minutes
JWT_EXPIRATION_MINUTES=60